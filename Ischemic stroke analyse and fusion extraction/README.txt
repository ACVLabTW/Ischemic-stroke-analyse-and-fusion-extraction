requirement_analyse contains the packages required for 'ischemic stroke analyze'

requirement_radiomics_and_image contains the packages required for 'fusion extraction' and 'radiomics extraction'

notice that errors may occur if the numpy version is greater than 1.21.6 .
install command : pip install -r requirements.txt
